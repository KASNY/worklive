{
  "name": "@worklive/desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf dist coverage"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@worklive/config": "workspace:*",
    "typescript": "catalog:"
  }
}

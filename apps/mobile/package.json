{
  "name": "@worklive/mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "clean": "rimraf .expo dist coverage"
  },
  "devDependencies": {
    "@worklive/config": "workspace:*",
    "typescript": "catalog:"
  }
}
